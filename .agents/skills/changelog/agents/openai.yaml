interface:
  display_name: "Changelog"
  short_description: "Repo-specific patterns and workflows for changelog"
  default_prompt: "Use the changelog repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true